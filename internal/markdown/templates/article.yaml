name: article
frontmatter:
  - title
  - url
  - source
  - author
  - published_at
  - extracted_at
  - extractor
  - language
  - word_count
  - category
  - tags

body:
  heading: "{{title}}"
  content: "{{content}}"
  footer: "Extracted on {{extracted_at}} via {{extractor}}"
